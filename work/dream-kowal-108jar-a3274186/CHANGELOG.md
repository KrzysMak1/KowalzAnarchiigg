# Changelog

## 1.0.8 (Paper 1.21.10)

- Dodano konfigurację projektu (Maven, Java 21) i pliki zasobów (`plugin.yml`, `config.yml`, `message.yml`).
- Usprawniono stabilność (bezpieczne sprawdzanie graczy, poziomów i efektów).
- Poprawiono działanie zadań oraz optymalizacje eventów.
